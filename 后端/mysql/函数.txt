			数学函数
abs(x)		//返回x的绝对值
sqrt(x)		//返回x的非负2次方根
mod(x,y)		//返回x被y除后的整数
ceiling(x)		//返回不小于x的最小整数
floor(x)		//返回不大于x的最大整数
round(x,y)	//对x进行四舍五入操作，小数点后保留y位
truncate(x,y)	//舍去x中的小数点y位后面的数
sign(x)		//返回x的符号，-1，0或者1
